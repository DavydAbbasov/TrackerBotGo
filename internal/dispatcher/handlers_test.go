package dispatcher

import "testing"

/*
Мок (Mock) — это подделка
настоящего объекта, которую мы используем в тестах.
Когда мы мокаем, мы создаём «фейковую» версию, которая:
не обращается к внешнему API,
не делает настоящих сетевых запросов,
но запоминает, что и как её вызвали.
*/
func TestHandleStart(t *testing.T) {

}
